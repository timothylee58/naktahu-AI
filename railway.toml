# Root railway.toml — points Railway at the correct service directory
# In Railway dashboard, set the service "Root Directory" to: apps/api
# The per-service railway.toml at apps/api/railway.toml controls build settings.
