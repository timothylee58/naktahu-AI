# Naktahu API

FastAPI service for `/api/v1/query` (SSE), session history, and Supabase JWT auth.

Run locally:

```bash
python -m uvicorn main:app --reload --app-dir .
```

Run tests:

```bash
python -m pytest tests/
```
